#ifndef __net_raid_server__
#define __net_raid_server__

// real_path should have enough space already allocated to
// write down corresponding string
char* gen_real_path(const char* logical_path);

#endif
